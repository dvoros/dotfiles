# GIT
alias gs='git status'
alias gr='git remote'
alias gl='git log --oneline --decorate=short --graph --date-order'
alias glf='git log --decorate=full --pretty=fuller'
alias glff='git log --decorate=full --pretty=fuller -p'
alias gd='git diff'
alias gdw='git diff --color-words=.'
alias grh='git reset --hard'
alias grhh='git reset --hard HEAD~'
alias grhu='git reset --hard @{u}'
alias gsno='git show --name-only'
alias gcp='git cherry-pick'
alias gcpx='git cherry-pick -x'

# Edit found stuff
alias v='vim $(fzf)'
alias vh='vim $(FZF_DEFAULT_COMMAND="find . -type f" fzf)'

# Launch remote tmux
tmux_at () {
	ssh -t $@ 'tmux -f <(wget -qO- https://raw.githubusercontent.com/dvoros/dotfiles/master/remote_tmux.conf)'
}
